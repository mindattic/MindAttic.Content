{
  "name": "websnapshot",
  "version": "1.0.0",
  "private": true,
  "description": "Capture web page screenshots, crop to a preview rectangle, write a base64 data URI, and render with a handheld-camera wobble.",
  "main": "web-snapshot.js",
  "bin": {
    "websnapshot": "snapshot.js"
  },
  "scripts": {
    "snapshot": "node snapshot.js",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
